# Daily ingest at 06:00 America/Santo_Domingo (set TZ on the container).
0 6 * * * python -m ingestion.ingestor
# Topic clustering over unprocessed raw_articles every 15 minutes.
*/15 * * * * python -m preprocessing.runner
